#include "function.h"

bool isXHasInArray(int a[], int n, int x) {
    for (int i = 0; i < n; ++i) {
        if (a[i] == x) {
            return true;
        }
    }
    return false;
}

void addX(int a[], int n) {
    for (int i = 1; i < n; ++i) {
    
        
    }
}
